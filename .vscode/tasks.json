{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "myLabel",
            "type": "shell",
            "command": "D:/program/mingw/bin/g++.exe",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/a.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}