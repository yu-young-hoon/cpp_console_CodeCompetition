#ifdef WIN32
#include "..\\stdc++.h"
#else
#include <bits/stdc++.h>
#endif

#define XX 65534
typedef long long ll;

using namespace std;

int main() {
	ll c;
	vector<ll> a;
	a.push_back(c);
	for (ll i = c-1; i <= 0; ++i) {
		if()
	}
	return 0;
}