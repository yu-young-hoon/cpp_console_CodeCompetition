#ifdef WIN32
#include "..\\stdc++.h"
#else
#include <bits/stdc++.h>
#endif

using namespace std;

int main() {
	int p, m;
	cin >> p >> m;
	vector<int> a;
	vector<int> b;

	int aa;
	for (int i = 0; i < p; ++i) {
		cin >> aa;
		a.push_back(aa);
	}

	int bb;
	for (int i = 0; i < p; ++i) {
		cin >> bb;
		b.push_back(bb);
	}

	(m + )

	for(int i = 0 ; i < m)

	return 0;
}